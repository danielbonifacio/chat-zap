import React, { Component } from 'react';
import { connect } from 'react-redux';

class ChatTrigger extends Component {
  render() {
    return (
      <div>
        asdkjhsdjksad
      </div>
    )
  }
}

const mapReducers = state => state; 

export default connect(mapReducers)(ChatTrigger);